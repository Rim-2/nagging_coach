# 잔소리 코치 — Docker(헤드리스 Linux 컨테이너)용 의존성.
#
# requirements.txt 에서 PC 활동 추적 패키지(pygetwindow, pynput, psutil)를 뺀 것.
# 컨테이너에는 데스크톱·입력장치가 없어 트래커가 동작할 수 없으므로
# (ENABLE_PC_TRACKER=false 고정) 해당 패키지는 설치하지 않는다.
# 코드도 트래커를 import 하지 않으니 런타임에 필요 없다.

# --- 코어 (헤드리스 텔레그램 봇) ---
google-genai>=1.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# --- Google 캘린더 연동 ---
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
